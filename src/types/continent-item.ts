export interface ContinentItem {
  continent: string;
  countriesCount: number;
  cases_new: string;
  cases_active: number;
  cases_critical: number;
  cases_recovered: number;
  cases_deaths: number;
}
